plotVar:
  parameters:
    endDate:
      label : 'end Date'
      type: date
      format: YYYY-DD-MM
    nameVar:
      label : 'variables Name'
      type: list
      values : listVariables
    smoothing:
      label : 'smoothing'
      type: boolean
    startDate:
      label : 'start Date'
      type: date
      format: YYYY-DD-MM
    token:
      type: string
      visibility: hidden
    wsUrl:
      type: string
      visibility: hidden
